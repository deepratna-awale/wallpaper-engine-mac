import Foundation

extension SceneEngineCombos {
    /// `HDR=1` on every material while the scene draws in HDR (`hdr`; docs/lighting-plan.md
    /// §2.6, 0x1401a6721). None yet.
    func hdrCombos(for material: [String: Int]) -> [String: Int] {
        [:]
    }
}
